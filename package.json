{
  "name": "dynmap-trains",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "cd testServer && ./run.sh nogui",
    "build": "esbuild --bundle --format=iife --external:leaflet --external:jquery --outfile=./build/trains.js ./src/index.ts",
    "generate-types": "tsc -p tsconfig.dynmap.json"
  },
  "devDependencies": {
    "@elfalem/leaflet-curve": "^0.9.2",
    "@types/jquery": "^3.5.16",
    "@types/leaflet": "^1.9.3",
    "bun-types": "^0.6.0",
    "esbuild": "^0.18.4",
    "typescript": "^5.1.3"
  }
}
